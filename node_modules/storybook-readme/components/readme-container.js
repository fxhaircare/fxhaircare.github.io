import React from 'react';
import Markdown from './markdown';
import './github-markdown-css';

var ReadmeContainer = function ReadmeContainer(_ref) {
  var markdown = _ref.markdown,
      style = _ref.style;

  var md = Array.isArray(markdown) ? markdown : [markdown];

  return React.createElement(
    'div',
    { className: 'markdown-body', style: style },
    md.map(function (source, index) {
      return React.createElement(Markdown, { source: source, key: index });
    })
  );
};

export default ReadmeContainer;